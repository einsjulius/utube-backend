services:
  - type: web
    name: utube-video-loader-backend
    runtime: python
    buildCommand: |
      pip install -r requirements.txt
      apt-get install -y ffmpeg || true
    startCommand: python server.py
    envVars:
      - key: PYTHON_VERSION
        value: "3.11.0"
